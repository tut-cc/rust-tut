[package]
name = "mandelbrot"
description = "GUI application for plotting the Mandelbrot set"
version = "0.1.0"
edition = "2024"

[dependencies.iced]
version = "0.14.0"
default-features = false
features = ["tiny-skia", "thread-pool", "x11", "wayland", "image"]
